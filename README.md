# John's Neovim configuration

Target: Neovim 0.13+

## Requirements

- `git`
- `clangd`
- `clang-format`
- C/C++ compiler/toolchain
- `curl`, `tar`, a C compiler, and `tree-sitter` CLI >= 0.26.1 for the current nvim-treesitter parser installation workflow
